package com.osacky.scan.tag;

import org.gradle.api.Plugin;

public class ScanApiPlugin implements Plugin<Object> {
    @Override
    public void apply(Object target) {
        // This plugin does nothing except serve as a marker to make it easy to add the API to your buildscript classpath
    }
}
